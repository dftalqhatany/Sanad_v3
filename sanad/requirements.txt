# Sanad Phase 2 (RAG adapter).
# The existing RAG keeps its own runtime dependencies in ../hr_assistant/requirements.txt;
# install those too when you want to run the real RAG (live tests, production).
pydantic>=2.6

# Phase 3 (document processing: PDF and DOCX only; no image/OCR dependencies in the MVP).
# PyMuPDF is the PDF library hr_assistant already uses (it pins 1.23.8).
pymupdf>=1.23.8
python-docx>=1.1

# tests
pytest>=8
# used by the contract tests, which execute the REAL legacy retrieval code (already listed in ../hr_assistant/requirements.txt)
numpy
rank-bm25
scikit-learn
qdrant-client

# Phase 7 (HTTP API): thin layer over SanadOrchestrator.handle()
fastapi>=0.115
uvicorn>=0.30
python-multipart>=0.0.9
httpx>=0.27

# Phase 7 (user interface): Streamlit, as in the existing hr_assistant app. It talks to the API over HTTP only.
streamlit>=1.36

"""Sanad user interface: a Streamlit app (app.py) plus its API client and view helpers.

The frontend communicates with the Sanad API over HTTP only; it never imports an agent, the
orchestrator, a parser or the RAG.
"""
